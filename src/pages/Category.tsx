import "./Category.scss";

const classes = {
    root: "CategoryPage"
}

export default function Category() {
    return (
        <main className={classes.root}>
            <h1>Category</h1>
        </main>
    )
}